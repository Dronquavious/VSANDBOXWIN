#ifndef CONSTANTS_H
#define CONSTANTS_H

// world settings
#define CHUNK_SIZE 32
#define RENDER_DISTANCE 12

// texture settings
#define BLOCK_TEX_SIZE 16

// helper macros
#define MIN(a,b) ((a) < (b) ? (a) : (b))

#endif