#ifndef CONSTANTS_H
#define CONSTANTS_H

// world settings
#define CHUNK_SIZE 64
#define RENDER_DISTANCE 8
#define SEA_LEVEL 28

// texture settings
#define BLOCK_TEX_SIZE 16

// helper macros
#define MIN(a,b) ((a) < (b) ? (a) : (b))

#endif